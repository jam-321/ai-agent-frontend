{
  "name": "ai-agent-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --mode development --host 127.0.0.1 --port 5173",
    "test": "vite --mode test --host 127.0.0.1 --port 5174",
    "build": "vite build --mode production",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "prod": "npm run build && vite preview --host 127.0.0.1 --port 4173"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  }
}
